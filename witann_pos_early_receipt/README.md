# Witann POS Early Receipt

Adds a per-POS setting to print a precuenta before payment without enabling the
Bar/Restaurant configuration. It delegates printing to the native POS receipt
pipeline and does not create a payment, invoice, or finalized POS order.
