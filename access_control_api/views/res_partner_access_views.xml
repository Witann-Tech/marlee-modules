<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <record id="view_partner_form_access_control" model="ir.ui.view">
      <field name="name">res.partner.form.access.control</field>
      <field name="model">res.partner</field>
      <field name="inherit_id" ref="base.view_partner_form"/>
      <field name="arch" type="xml">
        <xpath expr="//field[@name='image_1920'][1]" position="attributes">
          <attribute name="widget">face_camera</attribute>
        </xpath>
        <xpath expr="//notebook" position="inside">
          <page string="Control de Acceso">
            <group string="Validación de foto biométrica">
              <field name="access_person_face_preview" widget="image" readonly="1" class="oe_avatar"/>
              <div class="text-muted">
                La foto del contacto se captura en su ubicación original y aquí se valida la foto sincronizada al registro biométrico.
              </div>
            </group>
            <field name="access_person_ids" context="{'default_partner_id': id}" nolabel="1">
              <list editable="bottom" string="Personas de acceso">
                <field name="active"/>
                <field name="global_user_id"/>
                <field name="site_ids" widget="many2many_tags"/>
                <field name="has_face_pic" readonly="1"/>
              </list>
              <form string="Persona de acceso">
                <sheet>
                  <group>
                    <field name="active" widget="boolean_toggle"/>
                    <field name="global_user_id"/>
                    <field name="site_ids" widget="many2many_tags"/>
                    <field name="has_face_pic" readonly="1"/>
                  </group>
                  <group string="Foto de rostro (sincronizada desde Contacto)">
                    <field name="face_image" widget="image" readonly="1" class="oe_avatar"/>
                    <field name="face_pic_b64" invisible="1"/>
                  </group>
                </sheet>
              </form>
            </field>
          </page>
        </xpath>
      </field>
    </record>
  </data>
</odoo>
