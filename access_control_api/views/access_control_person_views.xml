<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <record id="view_access_control_person_tree" model="ir.ui.view">
      <field name="name">access_control.person.lista</field>
      <field name="model">access_control.person</field>
      <field name="arch" type="xml">
        <list string="Personas">
          <field name="active"/>
          <field name="partner_id"/>
          <field name="global_user_id" readonly="active"/>
          <field name="site_ids" widget="many2many_tags"/>
          <field name="has_face_pic"/>
        </list>
      </field>
    </record>

    <record id="view_access_control_person_form" model="ir.ui.view">
      <field name="name">access_control.person.formulario</field>
      <field name="model">access_control.person</field>
      <field name="arch" type="xml">
        <form string="Persona">
          <header>
            <button name="action_assign_global_user_id" type="object" string="Asignar ID global" class="btn-secondary" invisible="global_user_id"/>
            <button name="action_activate" type="object" string="Activar" class="btn-primary" invisible="active"/>
            <button name="action_deactivate" type="object" string="Desactivar" class="btn-secondary" invisible="not active"/>
          </header>
          <sheet>
            <group>
              <field name="active" widget="boolean_toggle"/>
              <field name="partner_id" required="1"/>
              <field name="global_user_id" readonly="active"/>
              <field name="site_ids" widget="many2many_tags" options="{'no_create_edit': False}"/>
              <field name="has_face_pic" readonly="1"/>
            </group>
            <group string="Foto de rostro">
              <field name="face_image" widget="image" readonly="1" class="oe_avatar"/>
              <field name="face_pic_b64" invisible="1"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <record id="view_access_control_person_search" model="ir.ui.view">
      <field name="name">access_control.person.buscar</field>
      <field name="model">access_control.person</field>
      <field name="arch" type="xml">
        <search string="Personas">
          <field name="partner_id"/>
          <field name="global_user_id"/>
          <field name="site_ids"/>
          <filter string="Con foto de rostro" name="with_face_pic" domain="[('face_pic_b64','!=',False)]"/>
          <filter string="Activos" name="active" domain="[('active','=',True)]"/>
          <filter string="Inactivos" name="inactive" domain="[('active','=',False)]"/>
        </search>
      </field>
    </record>
  </data>
</odoo>
